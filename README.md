# QR-Code-generator
use the below commands to download package and run the file

cmd:
1.npm i inquier
2.npm i qr-image
3.npm i node
to run the file 
node index.js

NOTE:
the output will be available in the image format in the folder structure
